name: befit
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.8.1


dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  shared_preferences: ^2.2.2
  flutter_svg: ^2.0.7
  fl_chart: ^0.66.0
  google_sign_in: ^6.2.1 # 👈 Downgrade to stable version
  cloud_firestore: ^4.13.5 # 👈 Firebase packages must be compatible
  cupertino_icons: ^1.0.8
  firebase_core: ^2.27.1
  firebase_auth: ^4.17.5
  animated_text_kit: ^4.2.1
  firebase_storage: ^11.6.8
  image_picker: ^1.1.0
  get: ^4.6.6
  table_calendar: ^3.0.9
  flutter_launcher_icons: ^0.13.1
  http: ^1.2.1
  flutter_screenutil: ^5.8.4
#  google_generative_ai: ^0.4.7
  chat_gpt_sdk: ^2.2.5
  dash_chat_2: ^0.0.20
  google_generative_ai: ^0.2.2
dev_dependencies:
  flutter_test:
    sdk: flutter


  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/
    - assets/images/
    - assets/Drawer_images/
    - assets/SuggestedWorkout_images/
    - assets/intake_images/
#    - assets/gradients/gradient_bg.png
    - assets/navbar_icons/dumbell_outlined.svg
    - assets/navbar_icons/dumbell.svg
    - assets/bmi_images/


flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/Drawer_images/img.png"